version: '3'

services:
  profile-web:
    command: node scripts/run_dev.js
    image: mozilla/fxa-profile-server
    ports:
      - "1111:1111"
  profile-worker:
    command: node bin/worker.js
    image: mozilla/fxa-profile-server
    ports:
      - "1113:1113"
